language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t sarathezil/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sarathezil/docker-react npm run test -- --coverage
  
